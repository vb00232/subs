<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <ul class="nav navbar-nav navbar-left">
      <li>
        <a class="navtitle" href="/">
          <img class="logo" src="/assets/LogoGrey.png" href="/">
          SUBS
        </a>
      </li>
      <% if user_signed_in? %>
        <li>
          <a href="/main">
            <%= t('navigation.main') %>
          </a>
        </li>
        <li>
          <%= render 'shared/search' %>
        </li>
      <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li>
          <div class="dropdown">
            <a><%= current_user.firstname %></a>
            <div class="dropdown-content">
              <a href="/products"><%= t('navigation.products') %></a>
              <a href="<%= edit_user_registration_path %>"><%= t('navigation.profile') %></a>
            </div>
          </div>
        </li>
        <li>
        </li>
        <li>
          <%= link_to t('navigation.logout'), destroy_user_session_path, method: 'delete' %>
        </li>
      <% else %>
        <li>
          <a href="<%= new_user_registration_path %>">
            <%= t('navigation.register') %>
          </a>
        </li>
        <li>
          <a href="<%= new_user_session_path %>">
            <%= t('navigation.login') %>
          </a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
